package com.montevideolabs.attractionsrecommender.common.feeds.visits

/**
  * Utility object defining the names of the columns in the visits feed.
  */
object VisitsColumnNames {

  val UserId = "user_id"
  val AttractionId = "attraction_id"
  val VisitsCount = "visits_count"
}
